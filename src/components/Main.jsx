import React from 'react'

const Main = () => {
  return (
    <main className="dark:bg-[var(--bg-main)] 
    h-full w-full
    dark:text-[var(--text-primary)] 
    bg-[var(--bg-main)] text-[var(--text-primary)] ">
    </main>  )
}

export default Main